import TTVideoControl from './tt-video/control';
import TTVideoPIPButton from './tt-video/pip-button';
import TTVideoLoading from './tt-video/loading';
import TTVideoError from './tt-video/error';
import TTVideoDurationText from './tt-video/duration-text';
import TTVideoFullscreenButton from './tt-video/fullscreen-button';
import TTVideoPlayButton from './tt-video/play-button';
import TTVideoPlaySwitch from './tt-video/play-switch';
import TTVideoPlaybackRateSwitch from './tt-video/playbackRate-switch';
import TTVideoProgress from './tt-video/progress';
import TTVideoQualitySwitch from './tt-video/quality-switch';
import TTVideoRotateButton from './tt-video/rotate-button';
import TTVideoScreenshotButton from './tt-video/screenshot-button';
import TTVideoSmallPlayButton from './tt-video/small-play-button';
import TTVideoVolume from './tt-video/volume';

export {
    TTVideoControl,
    TTVideoPIPButton,
    TTVideoLoading,
    TTVideoError,
    TTVideoDurationText,
    TTVideoFullscreenButton,
    TTVideoPlayButton,
    TTVideoPlaySwitch,
    TTVideoPlaybackRateSwitch,
    TTVideoProgress,
    TTVideoQualitySwitch,
    TTVideoRotateButton,
    TTVideoScreenshotButton,
    TTVideoSmallPlayButton,
    TTVideoVolume,
};

